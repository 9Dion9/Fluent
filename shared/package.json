{
  "name": "@fluent/shared",
  "version": "0.0.1",
  "private": true,
  "description": "Canonical JSON Schema contracts + fixtures for Fluent. Change here first.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "vitest": "^2.1.4"
  }
}
