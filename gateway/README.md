# gateway

FastAPI inference gateway (home GPU server). Built in **Milestone 1**:
`/v1/chat`, `/v1/tts`, `/v1/vision`, `/v1/generate`, `/healthz` wrapping Ollama + Piper + ffmpeg.

Not implemented yet — this directory is a placeholder so `worker/`'s `GATEWAY_URL`
has somewhere real to eventually point.
